# Native Runtime Example

This example demonstrates the Phase 11 first-party native runtime path for `bca2p.native`.

Files:

- [app.py](./app.py)

The example runs the research swarm graph directly through `NativeBioRuntime` without relying on LangGraph or LangChain execution.
