# Distributed Mesh Example

This example demonstrates the experimental SDK-owned substrate in `bca2p.distributed`.

Run it with:

```bash
PYTHONPATH=src python3 examples/distributed_mesh/app.py
```

The example shows:

- registry-backed agent discovery
- topology-aware routing
- partition simulation
- replay from the persisted signal log
