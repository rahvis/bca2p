# `bca2p.native`

`bca2p.native` provides the first-party runtime path that can execute `bca2p` graphs without LangGraph or LangChain.

## Main Objects

- `NativeCompiledBioGraph`
- `NativeBioRuntime`
