# `bca2p.integrations.langgraph`

`bca2p.integrations.langgraph` provides the adapter boundary between `BioGraph` and LangGraph `StateGraph`.

## Main Objects

- `LangGraphAdapter`
- `LangGraphChannelMapping`
- `LangGraphComplexBranch`
