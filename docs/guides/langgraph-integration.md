# LangGraph Integration Guide

The LangGraph adapter maps `BioGraph` channels and nodes onto a LangGraph-style `StateGraph`.

## Covered in Phase 8

- channel mapping
- state key mapping
- signal node wrappers
- complex branch representation
- checkpointer passthrough
